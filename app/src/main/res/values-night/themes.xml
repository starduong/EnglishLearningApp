<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.EnglishLearningApp" parent="Theme.Material3.Dark.NoActionBar">

        <!-- Primary -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Background -->
        <item name="android:colorBackground">@color/black</item>

        <!-- Surface -->
        <item name="colorSurface">@color/gray_900</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="colorSurfaceVariant">@color/gray</item>
        <item name="colorOnSurfaceVariant">@color/light_gray</item>

        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/gray_900</item>

    </style>

</resources>
